# 검증 기록 — 2026-09-10

## 1.1.0 Node.js 전환 검증

- Node.js 22.18.0에서 자동 테스트 통과: 바이트코드·점프·압축·해시 연결, 적용·복원, 설치 중 실행 감지, 두 번째 파일 교체 실패 복구, 외부 변경 보존, 기존 Python 백업 형식, 실행기 정상/오류/Node 미설치 처리.
- Python, `.venv`, `node_modules`, 개발용 소스가 없는 별도 배포 폴더를 만들고 Node.js와 Windows 기본 유틸리티만 PATH에 남겨 시험했다. 한글·공백이 포함된 도구/게임 복사본 경로에서 `run.bat --version`, CLI 상태 확인·적용·중복 적용 차단·복원을 통과했다.
- 실제 게임 파일의 격리 복사본을 적용·복원한 뒤 UPK와 EXE SHA-256이 모두 원본과 일치했다. 실게임 설치 폴더·DB·세이브에는 쓰지 않았다.
- Python 1.0.0 패치 결과와 Node.js 결과의 **286개 논리 청크가 모두 일치**했다. 기존 데이터 변경 8바이트, 추가 함수 315바이트, 스크립트 저장 길이 252 / 실행 길이 340 유지. 다른 가드·워프 코드도 보존됐다.
- 이전 Python 패치 결과와 버전 1 백업 기록을 새 배포 폴더에 넣어 상태 인식과 원본 복원도 확인했다.
- 별도로 이전 Python/lzokay 검사기로 Node.js 출력의 286개 청크를 읽고 원본 대비 변경 범위와 EXE SHA-1 연결 두 항목을 검증했다. 새 압축기와 새 해제기만 서로 비교한 결과가 아니다.

Node.js 출력 UPK SHA-256 (아래 1.0.0과 동일 원본 입력):
`c444c72e8f844c69ed77d47948b24260cd19c60cb4def685adbce28af3977838`

압축기 변경으로 전체 파일 해시는 달라지지만, 압축을 푼 패치 데이터는 Python 출력과 같다. 사용자 실게임 확인은 **1.0.0**에 대한 확인이며, 1.1.0은 이번 자동·복사본·독립 디코더 검증 결과다. 1.1.0을 새로 실게임 플레이했다고 주장하지 않는다.

재현 명령 (게임 원본 파일은 저장소에 포함하지 않음):

```powershell
npm test
node dev/integration.js "패치 전 게임 복사본 폴더" "기존 Python trial 출력 폴더"
node dev/verify.js ORIGINAL.upk NODE_PATCHED.upk ORIGINAL.exe NODE_PATCHED.exe
```

통합 검증은 `.test-output/node-integration-*` 안에만 쓰고 입력 게임 파일은 읽기만 한다. 실제 실행 여부 확인을 위해 Windows 프로세스 조회 권한이 필요하며 게임이 켜져 있으면 적용을 차단한다. Python 비교기는 개발용 선택 사항이며 일반 실행과 Node.js 테스트에는 필요하지 않다.

## 1.0.0 검증 기록

## 정식 배포 근거

사용자가 2026-09-10 실게임 테스트를 완료하고 문제가 없다고 확인했다. 해당 확인에 따라 1.0.0 정식 버전으로 전환한다. 나이프 발사 패치 생성 로직은 0.1.0에서 검증한 코드 그대로이며, 버전·사용 안내·배포 문구만 정리했다. 이는 사용자의 플레이 환경에서 확인된 결과이며 아래 모든 세부 조합을 각각 측정했다는 뜻은 아니다.

## 완료한 자동 검증

- 단위 테스트 10개 통과: 코드 길이/점프 재배치, 잘못된 토큰/분기 거부, LZO 압축 왕복, EXE 해시 항목만 변경, 백업 적용·복원, 두 번째 파일 교체 실패 롤백, 실행 중/외부 변경 차단, 손상 백업 차단, 중간 종료 복원.
- 설치된 게임의 두 파일을 별도 게임 폴더 복사본으로 만들어 실제 `apply`/`restore` 경로를 실행했다. 복원 후 UPK·EXE의 SHA-256이 원본과 일치했다. 중복 적용은 수정 전에 차단됐다.
- 패키지 286개 청크를 논리 데이터로 비교했다. 기존 데이터에서는 PlayShot export의 크기·오프셋 8바이트만 변경하며 마지막 청크에 315바이트짜리 새 함수를 추가한다. 다른 함수·워프·가드 데이터는 동일하다.
- EXE는 `brggame.upk` 매니페스트 SHA-1 두 항목만 변경한다.
- 새 함수의 저장 스크립트 길이 252바이트, 64비트 실행 길이 340바이트를 구분해 검증했다. UELib으로 패치 결과를 다시 디컴파일해 플레이어 분기와 원래 AI 분기를 확인했다.
- 최초 검증은 Python 3.12 분석 환경에서 수행하고, 프로젝트 전용 Python 3.13 가상환경에 고정 의존성 `lzokay==2.1.0`을 설치해 테스트를 다시 실행했다.
- 자동 검증 과정에서는 원본 게임 파일, 세이브, DB를 수정하지 않았다. 생성된 게임 파일 복사본과 가상환경은 Git에서 제외한다.

분석에 사용한 로컬 UPK SHA-256:
`db249934dac1ed486755dceac9d5a3b0be4ff9ec5370dca0e7e402c0018d3aaa`

해당 파일로 만든 패치 복사본 SHA-256:
`cb3e943a6f5084f6d3b71bab848df2293a5f8ef077b549d52dbb48ec93fce217`

이 전체 해시는 해당 로컬 조합의 검증 기록이다. 배포 도구는 핵심 원본 함수의 정확한 해시·export 위치·압축 구조·실행 파일 연결을 검사하며, 확인되지 않은 함수 버전은 수정하지 않는다.

## 세부 조합별 확인 범위

사용자는 실게임 정상 작동을 확인했지만, 다음 항목별 상세 테스트 결과를 별도로 제공하지는 않았다. 전수 검증 목록으로 주장하지 않으며 환경이 달라질 때의 재확인 목록으로 남긴다.

- 일반 M2G/FireFly에서 조준·비조준 나이프 발사가 실제로 나오는지
- 연속 사격, 낮은 잔탄, 탄약 소진, 재장전, 파손, 무기 교체
- AI M2G/레드 네이팜의 원래 공격 유지
- 현재 저스트가드/워프 조합의 실제 플레이
- 룰렛 그림이 돌아가는 알려진 시각적 불일치

룰렛 그림이 회전하는 시각적 차이는 알려진 동작으로 안내한다. 다른 게임 빌드·모드 조합까지 정상 동작을 보장하지 않는다.
