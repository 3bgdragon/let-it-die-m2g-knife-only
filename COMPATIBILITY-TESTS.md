# 세 도구 병용 검증 — 2026-09-11

대상: M2G 1.3.0, 저스트가드 1.7.0-dev, 워프 1.3.0-dev.

## 결과

- 25136512 및 확인된 25244463 입력에서 각각 GWM/GMW/WGM/WMG/MGW/MWG 6개 순서를 검증했다. G는 가드, W는 워프, M은 M2G다.
- 각 순서의 최종 파일 5개 SHA-256이 동일했다. 각각 역순으로 선택 제거한 결과도 초기 상태와 정확히 일치했다.
- 첫 도구 적용 직후의 전체 복원은 통과했다. 이후 다른 두 도구를 적용한 뒤 첫 도구의 과거 전체 백업 복원은 모두 거부했고, 거부 전후 파일 해시가 같았다.
- 새 빌드의 가드 4개 강도 × 4개 런타임 조합 × 워프 ON/OFF = 32개 설정 검증을 통과했다. 완화 0.85→0.5→0.85→순정 변환도 통과했다.
- 내장 M2G 8개 조합에서 제거 시 원래 PlayShot 함수로 연결되는지, 재적용 시 원본과 정확히 일치하는지 확인했다.
- 실제 설치 파일에는 쓰지 않았다. 이 결과는 실게임 전투 테스트를 대신하지 않는다.

## 조사 중 정정

`3877130C584B2CBABB89A22DF1E7A0E437496B37` 입력은 완전한 순정이 아니었다. PlayShot의 315바이트 함수가 기존 나이프 패치와 정확히 같았다. 원래 244바이트 함수도 남아 있어, 선택 제거는 내보내기 테이블의 함수 연결만 복구한다. 알 수 없는 파일은 허용하지 않는다.

## 재현

Node.js로 `node tests/run-three-tool-integration.js "원본 게임 폴더" "저스트가드 저장소" "워프 저장소"`를 실행한다. 모든 적용은 새 테스트 복사본에서 이루어지며, 사례별 임시 백업은 검증 뒤 정리하고 결과 JSON과 복사본을 남긴다.

내장 M2G 원본 fixture 8종이 있으면 `LID_M2G_EMBEDDED_FIXTURES` 환경 변수로 지정한 뒤 `node --test tests/embedded.test.js`를 실행한다. fixture 이름은 `guard-off-off.upk`, `guard-off-off-centered.upk` 형식이다.
